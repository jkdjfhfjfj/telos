kind = "web"
previewPath = "/"
title = "Telos Wallet"
version = "1.0.0"
id = "artifacts/telos-wallet"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20521

[services.development]
run = "pnpm --filter @workspace/telos-wallet run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/telos-wallet", "run", "build" ]
publicDir = "artifacts/telos-wallet/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20521"
BASE_PATH = "/"
